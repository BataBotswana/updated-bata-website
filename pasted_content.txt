{/* Compact category tiles */}
<section id="collections" className="collections-screen" aria-label="Collections">
  {[{ title: "Women", slug: "women", image: asset("women_image") }, { title: "Men", slug: "men", image: asset("men_image") }, { title: "Kids", slug: "kids", image: asset("kids_image") }].map((collection) => (
    <Link key={collection.slug} href={`/collections/${collection.slug}`} className="collection-screen-card">
      <img src={collection.image} alt={collection.title} loading="lazy" />
      <span>{collection.title}<ArrowUpRight size={19} /></span>
    </Link>
  ))}
</section>

{/* Responsive video campaign: replaces the Power/Toughees/Bata image slideshow */}
<section className="campaign-video" aria-label="Featured shoe campaign">
  <video
    className="campaign-video__media"
    autoPlay
    muted
    loop
    playsInline
    preload="metadata"
    poster={asset("campaign_image") || asset("power_image")}
  >
    <source src="https://batalebanon.com/cdn/shop/videos/c/vp/70a13a908ee141a3a256dc6b69568197/70a13a908ee141a3a256dc6b69568197.HD-1080p-7.2Mbps-79536968.mp4?v=0" type="video/mp4" />
  </video>
  <div className="campaign-video__shade" />
  <div className="campaign-video__copy">
    <span>Bata / In motion</span>
    <h2>Move in comfort.</h2>
    <Link href="/catalog" className="hero-slideshow__cta">Explore shoes <ArrowRight size={17} /></Link>
  </div>
</section>

{/* Smaller brand tiles in one horizontal row */}
<section id="brands" className="brands-screen" aria-label="Brands">
  {brands.map((brand) => (
    <Link key={brand.slug} href={`/collections/${brand.slug}`} className="brand-screen-card">
      <img src={brand.image} alt={`${brand.name} footwear`} loading="lazy" />
      <div className="brand-screen-card__wash" />
      {brand.logo ? <img className="brand-screen-card__logo" src={brand.logo} alt={brand.name} /> : <strong>{brand.name}</strong>}
      <ArrowUpRight className="brand-screen-card__arrow" size={18} />
    </Link>
  ))}
</section>

<section className="service-strip service-strip--minimal">
  <Link href="/stores"><Store size={18} /><span>Find a store</span></Link>
  <a href={String(import.meta.env.NEXT_PUBLIC_WHATSAPP_CATALOG_URL ?? "https://wa.me/c/26775303767")} target="_blank" rel="noreferrer"><ShoppingBag size={18} /><span>Shop WhatsApp</span></a>
  <button onClick={() => toast("Free delivery over P 800")}><MapPin size={18} /><span>Free delivery over P 800</span></button>
</section>

<section className="newsletter-section">
  <div>
    <p className="eyebrow">A step ahead</p>
    <h2 style={lineBreak}>{content("newsletter_heading", "The pair you want,\nbefore it goes.")}</h2>
  </div>
  <form onSubmit={(event) => { event.preventDefault(); toast.success("You’re on the list"); }}>
    <label htmlFor="newsletter">Email</label>
    <div className="newsletter-input"><input id="newsletter" type="email" placeholder="you@email.com" required /><button type="submit">Join us <ArrowRight size={18} /></button></div>
    <small>Unsubscribe anytime.</small>
  </form>
</section>

<footer className="site-footer site-footer--minimal">
  <BrandMark inverse logo={content("logo_url")} />
  <div><Link href="/collections/women">Women</Link><Link href="/collections/men">Men</Link><Link href="/collections/kids">Kids</Link><Link href="#brands">Brands</Link><Link href="/stores">Stores</Link></div>
  <small>© 2026 Bata Botswana</small>
</footer>

/* CSS additions */
.collections-screen { min-height: min(56svh, 560px); grid-template-rows: 1fr; }
.campaign-video { height: min(68svh, 680px); min-height: 420px; position: relative; overflow: hidden; background: #27221e; color: #fff; }
.campaign-video__media { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; }
.campaign-video__shade { position: absolute; inset: 0; background: linear-gradient(90deg, rgba(16,15,14,.78), rgba(16,15,14,.12) 68%, rgba(16,15,14,.18)); }
.campaign-video__copy { position: absolute; z-index: 1; left: clamp(22px, 8vw, 128px); bottom: clamp(36px, 8vw, 86px); }
.brands-screen { min-height: 0; grid-template-columns: repeat(4, minmax(0, 1fr)); grid-template-rows: none; gap: 8px; padding: clamp(22px, 4vw, 52px); }
.brand-screen-card { min-height: clamp(180px, 22vw, 280px); }
@media (max-width: 760px) {
  .collections-screen { min-height: 48svh; }
  .campaign-video { height: min(62svh, 520px); min-height: 360px; }
  .brands-screen { display: flex; flex-wrap: nowrap; overflow-x: auto; padding: 22px 16px 28px; gap: 8px; }
  .brand-screen-card { flex: 0 0 calc((100vw - 40px) / 2); min-height: 190px; }
}
